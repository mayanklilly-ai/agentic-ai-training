{
  "name": "Agentic AI Training",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-toolsai.vscode-jupyter-cell-tags",
        "ms-toolsai.jupyter-renderers"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "notebook.cellToolbarLocation": {
          "default": "right"
        }
      }
    }
  },
  "postCreateCommand": "pip install uv && uv pip install python-dotenv transformers sentence-transformers chromadb langchain langchain-community langgraph huggingface_hub matplotlib torch fastmcp httpx networkx nbformat nbconvert --index-url https://pypi.org",
  "remoteEnv": {
    "HF_TOKEN": "${localEnv:HF_TOKEN}"
  },
  "forwardPorts": [8765],
  "postAttachCommand": "echo Container ready. HF_TOKEN loaded: ${HF_TOKEN:0:6}..."
}
